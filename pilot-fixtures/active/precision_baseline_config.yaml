# baseline configuration
training_steps: 100
epochs: 4
batch_size: 8
dataset:
  identifier: precision-train
  version: v1
evaluation:
  dataset_identifier: precision-eval
  dataset_version: v1
  split: test
model: pilot-model-v1
learning_rate: 0.001
